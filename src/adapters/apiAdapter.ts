// src/adapters/apiAdapter.ts
import { adapterSupabase } from "./adapterSupabase";

// Aqui você pode futuramente trocar por adapterBackend, adapterEmpresa etc.
export const apiAdapter = adapterSupabase;
